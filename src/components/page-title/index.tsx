import React from 'react'
import { BreadCrumb, Container, SectionTitle, Title } from './styles'

const PageTitle = () => {
  return (
    <Container>
      <SectionTitle>
        <Title>Shop Grid Right Sidebar</Title>
        <BreadCrumb>Home / Grid</BreadCrumb>
      </SectionTitle>
    </Container>
  )
}

export default PageTitle
